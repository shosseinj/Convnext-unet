# Codex Master Prompt

Use `codex_agentic_workflow_v3/AGENTIC_WORKFLOW.md` as the binding workflow contract.

Repository root:
`C:\Users\jafari.h\Desktop\ai_project\ConvNeXt_Unet`

Execute the graph, not merely a plan.

First actions:

1. Stop all old repository-owned campaign, queue, watcher and training processes safely.
2. Confirm no repository GPU process remains.
3. Remove only stale locks after PID verification.
4. Preserve all previous results/checkpoints.
5. Run the ConvNeXt audit gate.
6. If PASS, implement and test UGBR and the composite loss.
7. Launch the seed-42 pilot matrix in one visible PowerShell terminal.
8. Tee terminal output to `CAMPAIGN_CONSOLE.log`.
9. Update only the concise `RUN_STATUS.md` for the user.
10. Stop Codex after verifying the visible launcher is healthy; do not poll epochs.

Required multi-agent behavior:

- Orchestrator delegates independent read-only audits in parallel.
- Write ownership is exclusive: only one agent may edit a file at a time.
- Training ownership is exclusive: only Experiment Agent launches GPU work.
- Validation Agent must independently validate every run.
- Review Agent must approve each graph transition.
- No agent may claim PASS without an evidence path.

Do not start manuscript or control work. Do not promise Dice > 0.94. Report actual evidence only.
